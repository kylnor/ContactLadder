import type { Metadata } from "next";
import { Lexend } from "next/font/google";
import "./globals.css";

const lexend = Lexend({
  variable: "--font-lexend",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "ContactLadder - Master Your CRM",
  description: "Complete training platform for real estate professionals. Learn Cloze CRM with comprehensive modules, video tutorials, and personalized learning.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${lexend.variable} antialiased font-sans`}
      >
        {children}
      </body>
    </html>
  );
}
